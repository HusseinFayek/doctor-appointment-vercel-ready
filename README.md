# DocBook - Vercel Ready

Run locally:

```
npm install
npm run dev
```

Deploy to Vercel with build command `npm run build` and output directory `dist`.

Doctor dashboard credentials: doctor / password123
